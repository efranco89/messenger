<%= turbo_frame_tag dom_id(message) do%>
  <div class="card">
    <div class="card-body">
      <p class="card-text">
        <strong>Item:</strong> <%= message.item %>
      </p>
      <%= link_to 'Edit', edit_message_path(message), class: "btn btn-danger btn-sm" %>
      <br>
    </div>
  </div>
<% end %>
